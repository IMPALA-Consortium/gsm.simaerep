# Handle all dfSubjects operations, only if GroupID is not yet in Numerator or Denominator frames. Will also filter all events with no GroupID

Handle all dfSubjects operations, only if GroupID is not yet in
Numerator or Denominator frames. Will also filter all events with no
GroupID

## Usage

``` r
AddGroupCol(df, dfSubjects, strSubjectCol, strGroupCol, strGroupLevel)
```
